# 等级、挡位（画质）与帧率关系说明

## 一、等级体系概览

系统存在 **两套等级**，帧率上限取两者的 **较小值**。

### 1. iOS 设备等级（`deviceLevel` / `memberActivationLevel`）

| 等级值 | 名称 | 说明 |
|:---:|:---:|:---:|
| 0 | 试用 | 全功能开放，等同最高等级 |
| 1 | 高清 | 基础等级 |
| 2 | 超清 | 中级等级 |
| 3 | 超高帧 | 高级等级 |
| 4 | 超超清 | 最高等级 |

> **来源**：登录接口 `/api/auth/login/control` 返回的 `deviceLevel` 字段

### 2. PC 端激活等级（`pcActivationLevel`）

| 等级值 | 名称 | 说明 |
|:---:|:---:|:---:|
| 0 | 未激活 | 限制帧率上限 |
| 1 | 豪华版 | 限制帧率上限 |
| 2 | AI全能版 | 不限制帧率 |

> **来源**：登录接口返回的 `pcLevel` / `pcLevelName` 字段

---

## 二、挡位（画质）一览

| UI 显示名 | 服务器 type | 分辨率 | 解锁所需 iOS 等级 |
|:---:|:---:|:---:|:---:|
| 超低网 | `low` | 640×480 | ≥ 1（或试用） |
| 高清 | `standard` | 1280×720 | ≥ 1（或试用） |
| 超清 | `high` | 1920×1080 | ≥ 2（或试用） |
| 超高清 | `p4k` | 3840×2160 | ≥ 3（或试用） |
| 超高帧 | `ultra` | 1920×1080（高帧率模式） | ≥ 4（或试用） |

> **试用（等级 0）**：所有挡位全部可用，无限制。

### 挡位可用性详细规则

| iOS 等级 | 可用挡位 |
|:---:|:---|
| 0（试用） | 超低网、高清、超清、超高清、超高帧 — **全部** |
| 1（高清） | 超低网、高清 |
| 2（超清） | 超低网、高清、超清 |
| 3（超高帧） | 超低网、高清、超清、超高清 |
| 4（超超清） | 超低网、高清、超清、超高清、超高帧 — **全部** |

---

## 三、帧率（FPS）上限规则

帧率滑块范围：**1 ~ 240**（服务器格式，实际显示帧率 = 服务器值 / 4）

### 3.1 所有挡位 — iOS 等级决定基础上限（动态获取）

> ⚠️ **此上限不再硬编码**，而是通过登录接口 `/api/auth/login/control` 返回的 `levelFps` 数组动态获取。
>
> `levelFps` 是一个整型数组，下标对应等级：`[试用, 高清, 超清, 超高清, 超高帧]`
>
> 例如 `levelFps: [30, 60, 120, 180, 240]` 表示：试用上限30、高清上限60、超清上限120、超高清上限180、超高帧上限240。

| iOS 等级 | 下标 | 基础帧率上限（示例默认值） | 数据来源 |
|:---:|:---:|:---:|:---:|
| 0（试用/日试用） | `levelFps[0]` | 240 | 登录接口 |
| 1（高清） | `levelFps[1]` | 120 | 登录接口 |
| 2（超清） | `levelFps[2]` | 180 | 登录接口 |
| 3（超高帧） | `levelFps[3]` | 180 | 登录接口 |
| ≥ 4（超超清） | `levelFps[4]` | 240 | 登录接口 |

**接口返回格式：**
```json
POST /api/auth/login/control
响应：
{
  "code": 0,
  "token": "...",
  "levelFps": [30, 60, 120, 180, 240],
  "...": "其他原有字段不变"
}
```

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `levelFps` | `int[]` | 各激活等级对应FPS上限值，下标0=试用, 1=高清, 2=超清, 3=超高清, 4=超高帧 |

> 若服务器未返回 `levelFps`，前端使用默认值 `[240, 120, 180, 180, 240]`。

### 3.3 PC 端等级额外限制

| PC 等级 | PC 帧率上限 |
|:---:|:---:|
| 0（未激活） | 120 |
| 1（豪华版） | 120 |
| 2（AI全能版） | 不限制（999） |

### 3.4 最终帧率上限 = min(iOS基础上限, PC帧率上限)

#### 完整计算示例表（假设 `levelFps = [240, 120, 180, 180, 240]`）

| 挡位 | iOS 等级 | PC 等级 | iOS 上限（来自 `levelFps`） | PC 上限 | **最终帧率上限** |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 超低网/高清 | 1 | 1（豪华） | `levelFps[1]`=120 | 120 | **120** |
| 超低网/高清 | 1 | 2（至尊） | `levelFps[1]`=120 | 999 | **120** |
| 超低网/高清 | 2 | 1（豪华） | `levelFps[2]`=180 | 120 | **120** |
| 超低网/高清 | 2 | 2（至尊） | `levelFps[2]`=180 | 999 | **180** |
| 超低网/高清 | 3 | 2（至尊） | `levelFps[3]`=180 | 999 | **180** |
| 超低网/高清 | 4 | 2（至尊） | `levelFps[4]`=240 | 999 | **240** |
| 超低网/高清 | 0(试用) | 1（豪华） | `levelFps[0]`=240 | 120 | **120** |
| 超低网/高清 | 0(试用) | 2（至尊） | `levelFps[0]`=240 | 999 | **240** |
| 超清 | 2 | 2（至尊） | `levelFps[2]`=180 | 999 | **180** |
| 超高清 | 3 | 2（至尊） | `levelFps[3]`=180 | 999 | **180** |
| 超高帧 | 4 | 2（至尊） | `levelFps[4]`=240 | 999 | **240** |
| 超高帧 | 3 | 2（至尊） | `levelFps[3]`=180 | 999 | **180** |
| 超高帧 | 0(试用) | 2（至尊） | `levelFps[0]`=240 | 999 | **240** |

> ⚠️ 以上 iOS 上限值仅为默认示例，实际值由后台 `levelFps` 字段决定。

---

## 四、超级帧率（Flicker/cjfps）上限规则

超级帧率滑块范围：**60 ~ 600**

### 4.1 iOS 等级决定基础上限（动态获取）

> ⚠️ **此上限不再硬编码**，而是通过登录接口 `/api/auth/login/control` 返回的 `levelExposureFps` 数组动态获取。
>
> `levelExposureFps` 是一个整型数组，下标对应等级：`[试用, 高清, 超清, 超高清, 超高帧]`
>
> 例如 `levelExposureFps: [30, 60, 120, 180, 240]` 表示：试用上限30、高清上限60、超清上限120、超高清上限180、超高帧上限240。

| iOS 等级 | 下标 | 基础超级帧率上限（示例默认值） | 数据来源 |
|:---:|:---:|:---:|:---:|
| 0（试用/日试用） | `levelExposureFps[0]` | 600 | 登录接口 |
| 1（高清） | `levelExposureFps[1]` | 120 | 登录接口 |
| 2（超清） | `levelExposureFps[2]` | 180 | 登录接口 |
| 3（超高帧） | `levelExposureFps[3]` | 240 | 登录接口 |
| ≥ 4（超超清） | `levelExposureFps[4]` | 600 | 登录接口 |

**接口返回格式：**
```json
POST /api/auth/login/control
响应：
{
  "code": 0,
  "token": "...",
  "levelFps": [30, 60, 120, 180, 240],
  "levelExposureFps": [30, 60, 120, 180, 240],
  "...": "其他原有字段不变"
}
```

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `levelFps` | `int[]` | 各激活等级对应FPS上限值，下标0=试用, 1=高清, 2=超清, 3=超高清, 4=超高帧 |
| `levelExposureFps` | `int[]` | 各激活等级对应超级帧率上限值，下标0=试用, 1=高清, 2=超清, 3=超高清, 4=超高帧 |

> 若服务器未返回 `levelExposureFps`，前端使用默认值 `[600, 120, 180, 240, 600]`。

### 4.2 PC 端等级额外限制

| PC 等级 | PC 超级帧上限 |
|:---:|:---:|
| 0（未激活） | 240 |
| 1（豪华版） | 240 |
| 2（AI全能版） | 不限制（999） |

### 4.3 最终超级帧率上限 = min(iOS基础上限, PC超级帧上限)

#### 完整计算示例表（假设 `levelExposureFps = [600, 120, 180, 240, 600]`）

| iOS 等级 | PC 等级 | iOS 上限（来自 `levelExposureFps`） | PC 上限 | **最终超级帧上限** |
|:---:|:---:|:---:|:---:|:---:|
| 1 | 1（豪华） | `levelExposureFps[1]`=120 | 240 | **120** |
| 1 | 2（至尊） | `levelExposureFps[1]`=120 | 999 | **120** |
| 2 | 1（豪华） | `levelExposureFps[2]`=180 | 240 | **180** |
| 2 | 2（至尊） | `levelExposureFps[2]`=180 | 999 | **180** |
| 3 | 1（豪华） | `levelExposureFps[3]`=240 | 240 | **240** |
| 3 | 2（至尊） | `levelExposureFps[3]`=240 | 999 | **240** |
| 4 | 1（豪华） | `levelExposureFps[4]`=600 | 240 | **240** |
| 4 | 2（至尊） | `levelExposureFps[4]`=600 | 999 | **600** |
| 0(试用) | 1（豪华） | `levelExposureFps[0]`=600 | 240 | **240** |
| 0(试用) | 2（至尊） | `levelExposureFps[0]`=600 | 999 | **600** |

> ⚠️ 以上 iOS 上限值仅为默认示例，实际值由后台 `levelExposureFps` 字段决定。

---

## 五、帧率四档阶梯（网络自适应）

网络不稳定时，系统会自动降低帧率，可用档位如下（服务器格式）：

| 阶梯 | 服务器值 | 实际帧率 |
|:---:|:---:|:---:|
| 第1档（最高） | 240 | 60fps |
| 第2档 | 180 | 45fps |
| 第3档 | 120 | 30fps |
| 第4档（最低） | 60 | 15fps |

> 只有当前帧率上限范围内的档位可用。例如上限120时，只有第3档（120）和第4档（60）可用。

---

## 六、挡位切换时帧率处理逻辑

1. 切换挡位时 **保持当前帧率和超级帧率不变**
2. 如果当前帧率 **超过新挡位上限**，自动限制到上限值
3. 如果当前超级帧率 **超过新挡位上限**，自动限制到上限值
4. 切换后触发 **PLI 关键帧请求**（防止绿幕），分 4 次发送（300ms/500ms/1000ms/1500ms）
5. 切换有 **1秒防抖**，1秒内不能连续切换

---

## 七、关键代码对照

| 功能 | 函数名 | 位置 |
|:---|:---|:---|
| 帧率上限计算 | `getMaxFpsForQuality(qualityType)` | MainPage.qml |
| 超级帧率上限计算 | `getMaxFlickerForQuality(qualityType)` | MainPage.qml |
| 挡位可用性检查 | `isQualityAccessible(qualityName)` | MainPage.qml |
| 挡位切换逻辑 | `switchQuality(qualityType, qualityName)` | MainPage.qml |
| UI名→服务器名 | `uiQualityToServerName(uiName)` | MainPage.qml |
| 帧率更新接口 | `HttpClient.updateFps(value)` | httpclient.cpp |
| 超级帧率更新接口 | `HttpClient.updateFlicker(value)` | httpclient.cpp |
| 挡位更新接口 | `HttpClient.updateQualityType(type)` | httpclient.cpp |

---

## 八、接口参数

### 帧率更新
- **HTTP**: `PUT /api/thin-config/{deviceId}?updatedBy={username}` → body: `{"fps": 120}`
- **WebSocket**: `CONFIG_UPDATE` → `{"fps": 120}`

### 超级帧率更新
- **HTTP**: `PUT /api/thin-config/{deviceId}?updatedBy={username}` → body: `{"cjfps": 180}`
- **WebSocket**: `CONFIG_UPDATE` → `{"cjfps": 180}`

### 挡位切换
- **HTTP**: `PUT /api/thin-config/{deviceId}?updatedBy={username}` → body: `{"type": "standard"}`
- **WebSocket**: `CONFIG_UPDATE` → `{"type": "standard"}`

### 服务器 type 值对照
| type | UI 名称 | 分辨率 |
|:---:|:---:|:---:|
| `low` | 超低网 | 640×480 |
| `standard` | 高清 | 800×600 |
| `high` | 超清 | 1440x1080 |
| `p4k` | 超高清 | 1920×1440 |
| `ultra` | 超高帧 | 1920×1080 |
